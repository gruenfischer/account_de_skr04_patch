[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
dynamic = ["name", "version", "description", "dependencies"]
readme = { file = "README.rst", content-type = "text/x-rst" }
requires-python = ">=3.9"
